require([
  //'fb',
  //tw',
  'jquery'
  ], function (
    //fb,
    //tw,
    $
  ) {
    var $doc = $(document);
    
    function init() {
      //fb.parseEls();
      //tw.parseEls();
    }
    $doc.ready(init);
});